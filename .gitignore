node_modules/
dist/
.turbo/
coverage/
*.log
.DS_Store
.env
.env.*
!.env.example
test-results/
playwright-report/
*.tsbuildinfo

# Local dev artifact storage (shared by the API + worker)
.varys-artifacts/

# Filled-in K8s secret (only the .example template is committed)
deploy/k8s/secret.yaml

# WXT extension build output
.wxt/
.output/

# Local Claude Code / skills tooling — not part of the project
.agents/
.claude/
skills-lock.json
