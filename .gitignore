node_modules/
dist/
.turbo/
coverage/
*.log
.DS_Store
.env
.env.*
!.env.example
test-results/
playwright-report/
*.tsbuildinfo

# Local dev artifact storage (shared by the API + worker)
.varys-artifacts/

# Never commit a rendered K8s Secret — the chart references an out-of-band secret
# created with `kubectl create secret`.
deploy/**/secret*.yaml

# WXT extension build output
.wxt/
.output/

# Local Claude Code / skills tooling — not part of the project
.agents/
.claude/
skills-lock.json
